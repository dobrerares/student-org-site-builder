# Implementation Plans

Generated by the improve skill on 2026-06-12, against commit `176e34e`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

> Selection note: this run was non-interactive, so the top 5 findings by
> leverage were planned by default. The vetted-but-unplanned backlog below
> lists everything else that survived verification — ask for any of them by
> name to get a plan written.

## Execution order & status

| Plan | Title                                                           | Priority | Effort | Depends on | Status |
| ---- | --------------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Cache pnpm store + Playwright browsers in CI                    | P2       | S      | —          | DONE   |
| 002  | Safe-URL-scheme validation for all five unvalidated link fields | P1       | M      | —          | DONE   |
| 003  | Zip import decompression limits (anti-bomb)                     | P1       | M      | —          | DONE   |
| 004  | Browser-shell Vite dev server (`pnpm dev`)                      | P1       | M      | —          | DONE   |
| 005  | Docs truth refresh (README/CONTRIBUTING + retire handoff files) | P2       | S      | 004        | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- 005 depends on 004: the rewritten CONTRIBUTING "Running locally" section
  documents the `pnpm dev` script that 004 creates. 005 carries a partial-
  execution fallback if 004 is not landed (its Step 2 precondition).
- 001–004 are mutually independent and can run in parallel worktrees.

## Vetted findings, not yet planned (ask to promote)

- **Theme pizzaz implementation plan** (direction): the design is locked in
  `docs/superpowers/specs/2026-05-28-themes-pizzaz-design.md` but no
  implementation plan exists. Should sequence: hero `eyebrow` schema removal
  first, then a shared theme-CSS scaffold (see next item), then per-theme
  signature work + golden refresh (5 themes × hero + 3 blocks).
- **Theme-CSS shared scaffolding** (tech-debt): the five files in
  `packages/renderer/src/themes/` repeat the same token-table + `:root` + CSS
  boilerplate; the pizzaz refresh multiplies the duplication. Best landed as
  the _first_ step of the pizzaz work, not standalone.
- **Theme-id registry consolidation** (tech-debt): three hand-maintained
  sources — `KNOWN_THEME_IDS` (`packages/renderer/src/index.tsx`),
  `KNOWN_THEME_IDS_FOR_VALIDATION` (`packages/schema/src/validate.ts`), and
  `THEME_METADATA` (`packages/themes/src/theme-catalog.ts:49`). The drift
  guard test covers only the first two; a forgotten catalog entry degrades
  silently (humanised-label fallback).
- **Alt-sync test gap** (tests, S): `packages/editor-app/test/alt-sync.test.ts`
  covers hero + gallery dual-write but not `events[].imageAlt ↔ events[].image.alt`
  nor `org.logoAlt ↔ org.logo.alt` (both implemented in
  `src/alt-sync.ts`'s mapping and currently correct — verified by read).
- **Markdown parser characterization tests** (tests, M):
  `packages/markdown/test/` is all safety-focused (escape/whitelist/
  xss-corpus); the block/inline parse _correctness_ (lists, blockquotes,
  heading boundaries, empty input) has no locked-in expectations.
- **Toolchain currency** (deps, M, MED risk): per `pnpm outdated` —
  vite 6.0.5→8.x, vitest 2.1.8→4.x, eslint 9→10, typescript 5.7→6.0,
  @playwright/test 1.49→1.60, esbuild 0.24→0.28. `pnpm audit --prod` shows
  3 critical / 2 moderate advisories confined to the dev-time
  vite/vitest/esbuild/ws chain — upgrading clears them. All devDependencies;
  nothing shipped is affected. Suggest one plan, staged: vitest+vite first,
  then eslint, then TS, with the green 1912-test baseline as the gate.
- **Preview-bridge receiver** (direction): `CONTEXT.md` documents the bridge
  as half-built (host posts envelopes; iframe has no listener — preview
  rebuilds full `srcdoc` per snapshot). Landing the receiver removes the
  full-document rebuild per keystroke and is the right moment to replace the
  `postMessage(envelope, "*")` target origin
  (`packages/preview-bridge/src/index.ts:150`) with a pinned origin.
- Carried over from MERGE_HANDOFF.md: sweep bare "ADR NNNN" references for
  staleness after the 0032–0041 renumbering (~30 sites, cosmetic). Not
  planned — low impact; grep `ADR 00(2[3-9]|3[0-1])` and spot-check if it
  ever matters.

## Findings considered and rejected

- Preview-bridge missing-integration-test claim: rejected —
  `packages/editor-app/test/edit-propagation.test.tsx` already spies on
  `iframe.contentWindow.postMessage` and asserts posted envelopes.
- `preview-bridge` docstring "scope confusion": rejected — the module comment
  (`src/index.ts:129-136`) already states the helper is host-side only.
- Form-renderer extraction (SpineForm/BlockForm shared `FieldRenderer`):
  not worth doing now — the separation is deliberate per ADR 0043; revisit
  only if a third schema-walking form appears.
- `editor-app.tsx` decomposition (1109 lines): not worth doing now — HIGH
  refactor risk, no current feature pain; revisit when feature work in the
  file starts colliding.
- Editor-state `structuredClone` benchmark: rejected — no symptom; typical
  Site JSON is small and the 200ms preview SLA is met; fold measurement into
  the preview-receiver work if pursued.
- Preact caret-vs-exact pinning: rejected — pnpm workspace resolution
  converges; pure hygiene with no failure mode observed.
- Wizard coverage gap claim: downgraded/rejected as stated — the package has
  five targeted suites (state-machine, persistence, resume-flow, ui,
  build-site); the "asset upload untested" part was wrong (the wizard mounts
  no pickers, by design).
- `postMessage` targetOrigin as a standalone security fix: folded into the
  preview-bridge receiver direction item above (srcdoc iframe + envelope
  validation make it defense-in-depth today, not an exposure).
- Per-keystroke `srcdoc` rebuild as a standalone perf fix: folded into the
  preview-bridge receiver direction item (the rebuild _is_ the documented
  interim mechanism).
- Social-URL finding's "Preact escapes javascript: hrefs" claim (from the
  audit pass): corrected during vetting — attribute escaping does **not**
  neutralize scheme-based `href` payloads; that's why plan 002 exists.
- `pnpm audit` dev-chain advisories as a standalone security finding:
  folded into the toolchain-currency backlog item (not reachable from
  shipped artifacts).
