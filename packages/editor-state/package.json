{
  "name": "@sosb/editor-state",
  "version": "0.0.0",
  "private": true,
  "description": "Live document model with debounced auto-save to a VFS.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "build": "tsc --build"
  },
  "dependencies": {
    "@sosb/schema": "workspace:*",
    "@sosb/vfs": "workspace:*",
    "@sosb/zip": "workspace:*"
  }
}
