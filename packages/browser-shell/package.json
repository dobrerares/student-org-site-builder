{
  "name": "@sosb/browser-shell",
  "version": "0.0.0",
  "private": true,
  "description": "Service worker, single-file archival build, OPFS bootstrap, hosted-deployment artifact.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --build"
  }
}
