# @sosb/themes

Theme catalog, token defaults, and curated starter templates.

The renderer consumes the theme ids and token defaults from this package. The
welcome shell also reads the curated template registry for the "ready-made
example" start path.

See `docs/PRD.md` for the canonical scope of this module.
