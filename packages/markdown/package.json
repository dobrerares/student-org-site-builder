{
  "name": "@sosb/markdown",
  "version": "0.0.0",
  "private": true,
  "description": "Strict-whitelist sanitised markdown. Used by richText, faq, quote.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "build": "tsc --build"
  }
}
