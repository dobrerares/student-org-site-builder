{
  "name": "@sosb/build",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-runnable build pipeline: (siteData) -> dist folder. Wraps @sosb/renderer and emits robots.txt + sitemap.xml.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "build": "tsc --build"
  },
  "dependencies": {
    "@sosb/renderer": "workspace:*",
    "@sosb/schema": "workspace:*",
    "fflate": "0.8.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
