{
  "name": "@sosb/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Block + site schemas, validation with severity tiers, migration framework, preserve-unknown-keys.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --build"
  }
}
