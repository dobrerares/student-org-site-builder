{
  "name": "@sosb/editor-app",
  "version": "0.0.0",
  "private": true,
  "description": "Preact UI composing the deep modules (forms, block list, preview pane, validation panel).",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --build"
  }
}
